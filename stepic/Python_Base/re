Функции:
match - проверяет строку на шаблон
search - находит первое вхождение шаблона
findall - находит все вхождения шаблона
sub - заменяет все вхождения шаблона на другую строку

[] - диапазон символов ([0-9], [a-z])
\d = [0-9] - цифры
\D = [^0-9] - НЕ цифры
\s = [ \t\n\r\f\v] - пробельные символы (пробел в начале)
\S = [^ \t\n\r\f\v] - НЕ пробельные символы
\w = [a-zA-Z0-9_] - буквы и цифры и _
\W = [^a-zA-Z0-9_] - HE буквы и цифры и _
. = один любой символ кроме \n
* = ноль или более повторений символа, стоящего перед *
+ = одно или более повторений символа, стоящего перед +
? = ноль или один повторений символа, стоящего перед ?
{n} = n повторений символа, стоящего перед {n}
{n, m} = от n до m повторений символа, стоящего перед {n, m}
() = группа символов
| = или (a|b = выбери а или b) 

